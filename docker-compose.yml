services:
  app:
    build: .
    container_name: aws-python-platform-template
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: uvicorn platform_service.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./src:/app/src
